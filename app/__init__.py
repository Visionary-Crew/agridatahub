"""
This package contains general utility modules and shared functions.

- Place code here that is used across the application but does not fit elsewhere.
- Example: string manipulation, config loading helpers, etc.

Usage:
    from app.utils.string_utils import slugify
"""
